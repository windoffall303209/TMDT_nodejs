<%- include('../partials/admin-header', { currentPage: 'sales' }) %>

<div class="admin-page">
    <div class="container">
        <h1 class="admin-page__title">🏷️ Quản Lý Khuyến Mãi</h1>
        
        <!-- Add Sale Form -->
        <div class="admin-section">
            <h3 class="admin-section__title">➕ Thêm Khuyến Mãi</h3>
            <form action="/admin/sales" method="POST">
                <div class="admin-form__grid">
                    <div>
                        <label class="admin-form__label">Tên*</label>
                        <input type="text" name="name" required class="admin-form__input">
                    </div>
                    <div>
                        <label class="admin-form__label">Loại</label>
                        <select name="type" class="admin-form__select">
                            <option value="percentage">Phần trăm (%)</option>
                            <option value="fixed">Số tiền (VNĐ)</option>
                        </select>
                    </div>
                    <div>
                        <label class="admin-form__label">Giá trị*</label>
                        <input type="number" name="value" required step="0.01" class="admin-form__input">
                    </div>
                    <div>
                        <label class="admin-form__label">Ngày bắt đầu</label>
                        <input type="datetime-local" name="start_date" class="admin-form__input">
                    </div>
                    <div>
                        <label class="admin-form__label">Ngày kết thúc</label>
                        <input type="datetime-local" name="end_date" class="admin-form__input">
                    </div>
                </div>
                <button type="submit" class="admin-form__submit">Thêm Khuyến Mãi</button>
            </form>
        </div>
        
        <% if (sales && sales.length > 0) { %>
            <div class="admin-table-wrapper">
                <table class="admin-table">
                    <thead>
                        <tr>
                            <th>ID</th>
                            <th>Tên</th>
                            <th class="text-center">Loại</th>
                            <th class="text-center">Giá trị</th>
                            <th class="text-center">Trạng thái</th>
                        </tr>
                    </thead>
                    <tbody>
                        <% sales.forEach(sale => { %>
                            <tr>
                                <td><%= sale.id %></td>
                                <td><strong><%= sale.name %></strong></td>
                                <td class="text-center"><%= sale.type === 'percentage' ? 'Phần trăm' : 'Cố định' %></td>
                                <td class="text-center admin-table__price">
                                    <%= sale.type === 'percentage' ? '-' + sale.value + '%' : '-' + (sale.value || 0).toLocaleString('vi-VN') + 'đ' %>
                                </td>
                                <td class="text-center">
                                    <span class="admin-table__badge" style="background: <%= sale.is_active ? '#e8f5e9' : '#ffebee' %>; color: <%= sale.is_active ? '#2e7d32' : '#c62828' %>;">
                                        <%= sale.is_active ? 'Hoạt động' : 'Hết hạn' %>
                                    </span>
                                </td>
                            </tr>
                        <% }) %>
                    </tbody>
                </table>
            </div>
        <% } else { %>
            <div class="admin-empty">
                <span class="admin-empty__icon">🏷️</span>
                <h3 class="admin-empty__title">Chưa có khuyến mãi</h3>
            </div>
        <% } %>
    </div>
</div>

<%- include('../partials/admin-footer') %>
