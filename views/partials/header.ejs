<!DOCTYPE html>
<html lang="vi">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>WIND OF FALL - Thời Trang Cao Cấp</title>
    <link rel="stylesheet" href="/css/style.css">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&display=swap" rel="stylesheet">
</head>
<body>

<header class="header">
    <div class="container">
        <!-- Header: Logo - Nav - Search - Actions (một dòng) -->
        <div class="header-main">
            <div class="logo">
                <a href="/">WIND OF FALL</a>
            </div>

            <!-- Main Navigation - Nằm cùng header -->
            <nav class="main-nav">
                <%
                    const currentPath = typeof path !== 'undefined' ? path : '';
                    const currentCategory = typeof category !== 'undefined' ? category : null;
                    const categorySlug = currentCategory ? currentCategory.slug : '';
                    const isSalePageActive = typeof isSalePage !== 'undefined' ? isSalePage : currentPath.includes('sale=true');
                %>
                <a href="/products/category/nam" class="<%= categorySlug === 'nam' ? 'active' : '' %>">Nam</a>
                <a href="/products/category/nu" class="<%= categorySlug === 'nu' ? 'active' : '' %>">Nữ</a>
                <a href="/products/category/tre-em" class="<%= categorySlug === 'tre-em' ? 'active' : '' %>">Trẻ em</a>
                <a href="/products?sale=true" class="sale-link <%= isSalePageActive ? 'active' : '' %>">Sale</a>
            </nav>

            <div class="header-search">
                <form action="/products/search" method="GET">
                    <input type="text" name="q" placeholder="Tìm kiếm sản phẩm..." required>
                    <button type="submit">
                        <svg width="18" height="18" viewBox="0 0 20 20" fill="none">
                            <path d="M9 17A8 8 0 1 0 9 1a8 8 0 0 0 0 16zM18 18l-4.35-4.35" stroke="currentColor" stroke-width="2" stroke-linecap="round"/>
                        </svg>
                    </button>
                </form>
            </div>

            <div class="header-actions">
                <% if (user) { %>
                    <% if (user.role === 'admin') { %>
                        <a href="/admin/dashboard" class="admin-badge" title="Admin Dashboard">Admin</a>
                    <% } %>
                    <a href="/auth/profile" class="icon-btn" title="Tài khoản">
                        <svg width="20" height="20" viewBox="0 0 24 24" fill="none">
                            <path d="M20 21v-2a4 4 0 0 0-4-4H8a4 4 0 0 0-4 4v2M12 11a4 4 0 1 0 0-8 4 4 0 0 0 0 8z" stroke="currentColor" stroke-width="2" stroke-linecap="round"/>
                        </svg>
                    </a>
                    <a href="/orders/history" class="icon-btn" title="Đơn hàng">
                        <svg width="20" height="20" viewBox="0 0 24 24" fill="none">
                            <path d="M9 5H7a2 2 0 0 0-2 2v12a2 2 0 0 0 2 2h10a2 2 0 0 0 2-2V7a2 2 0 0 0-2-2h-2M9 5a2 2 0 0 0 2 2h2a2 2 0 0 0 2-2M9 5a2 2 0 0 1 2-2h2a2 2 0 0 1 2 2M9 12h6M9 16h6" stroke="currentColor" stroke-width="2" stroke-linecap="round"/>
                        </svg>
                    </a>
                    <a href="/cart" class="icon-btn cart-btn" title="Giỏ hàng">
                        <svg width="20" height="20" viewBox="0 0 24 24" fill="none">
                            <path d="M6 2L3 6v14a2 2 0 0 0 2 2h14a2 2 0 0 0 2-2V6l-3-4z" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/>
                            <line x1="3" y1="6" x2="21" y2="6" stroke="currentColor" stroke-width="2"/>
                            <path d="M16 10a4 4 0 0 1-8 0" stroke="currentColor" stroke-width="2" stroke-linecap="round"/>
                        </svg>
                        <span class="cart-count" id="cart-count">0</span>
                  </a>
                    <a href="/auth/logout" class="icon-btn" title="Đăng xuất" style="color: var(--accent);">
                        <svg width="20" height="20" viewBox="0 0 24 24" fill="none">
                            <path d="M9 21H5a2 2 0 0 1-2-2V5a2 2 0 0 1 2-2h4M16 17l5-5-5-5M21 12H9" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/>
                        </svg>
                    </a>
                <% } else { %>
                    <a href="/auth/login" class="auth-link" title="Đăng nhập">
                        <svg width="20" height="20" viewBox="0 0 24 24" fill="none">
                            <path d="M20 21v-2a4 4 0 0 0-4-4H8a4 4 0 0 0-4 4v2M12 11a4 4 0 1 0 0-8 4 4 0 0 0 0 8z" stroke="currentColor" stroke-width="2" stroke-linecap="round"/>
                        </svg>
                        <span>Đăng nhập / Đăng ký</span>
                    </a>
                <% } %>

                <!-- Mobile Menu Button -->
                <button class="mobile-menu-btn" id="mobileMenuBtn" aria-label="Menu">
                    <span></span>
                    <span></span>
                    <span></span>
                </button>
            </div>
        </div>

        <!-- Mobile Search Bar - Always visible on mobile -->
        <div class="mobile-header-search">
            <form action="/products/search" method="GET">
                <input type="text" name="q" placeholder="Tìm kiếm sản phẩm..." required>
                <button type="submit">
                    <svg width="18" height="18" viewBox="0 0 20 20" fill="none">
                        <path d="M9 17A8 8 0 1 0 9 1a8 8 0 0 0 0 16zM18 18l-4.35-4.35" stroke="currentColor" stroke-width="2" stroke-linecap="round"/>
                    </svg>
                </button>
            </form>
        </div>
    </div>
</header>

<!-- Mobile Navigation Overlay -->
<div class="mobile-nav-overlay" id="mobileNavOverlay"></div>

<!-- Mobile Navigation Drawer -->
<nav class="mobile-nav" id="mobileNav">
    <div class="mobile-nav__header">
        <span class="mobile-nav__logo">WIND OF FALL</span>
        <button class="mobile-nav__close" id="mobileNavClose" aria-label="Đóng menu">×</button>
    </div>

    <div class="mobile-nav__search">
        <form action="/products/search" method="GET">
            <input type="text" name="q" placeholder="Tìm kiếm sản phẩm...">
            <button type="submit">
                <svg width="18" height="18" viewBox="0 0 20 20" fill="none">
                    <path d="M9 17A8 8 0 1 0 9 1a8 8 0 0 0 0 16zM18 18l-4.35-4.35" stroke="currentColor" stroke-width="2" stroke-linecap="round"/>
                </svg>
            </button>
        </form>
    </div>

    <div class="mobile-nav__links">
        <%
            const mobileCurrentPath = typeof path !== 'undefined' ? path : '';
            const mobileCurrentCategory = typeof category !== 'undefined' ? category : null;
            const mobileCategorySlug = mobileCurrentCategory ? mobileCurrentCategory.slug : '';
            const mobileIsSalePageActive = typeof isSalePage !== 'undefined' ? isSalePage : mobileCurrentPath.includes('sale=true');
        %>
        <a href="/" class="<%= mobileCurrentPath === '/' ? 'active' : '' %>">
            <svg width="20" height="20" viewBox="0 0 24 24" fill="none"><path d="M3 9l9-7 9 7v11a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2z" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/></svg>
            Trang chủ
        </a>
        <a href="/products/category/nam" class="<%= mobileCategorySlug === 'nam' ? 'active' : '' %>">Nam</a>
        <a href="/products/category/nu" class="<%= mobileCategorySlug === 'nu' ? 'active' : '' %>">Nữ</a>
        <a href="/products/category/tre-em" class="<%= mobileCategorySlug === 'tre-em' ? 'active' : '' %>">Trẻ em</a>
        <a href="/products?sale=true" class="sale-link <%= mobileIsSalePageActive ? 'active' : '' %>">Sale</a>
    </div>

    <div class="mobile-nav__actions">
        <% if (user) { %>
            <% if (user.role === 'admin') { %>
                <a href="/admin/dashboard" class="admin-badge">Admin Dashboard</a>
            <% } %>
            <a href="/auth/profile">
                <svg width="20" height="20" viewBox="0 0 24 24" fill="none"><path d="M20 21v-2a4 4 0 0 0-4-4H8a4 4 0 0 0-4 4v2M12 11a4 4 0 1 0 0-8 4 4 0 0 0 0 8z" stroke="currentColor" stroke-width="2" stroke-linecap="round"/></svg>
                Tài khoản của tôi
            </a>
            <a href="/orders/history">
                <svg width="20" height="20" viewBox="0 0 24 24" fill="none"><path d="M9 5H7a2 2 0 0 0-2 2v12a2 2 0 0 0 2 2h10a2 2 0 0 0 2-2V7a2 2 0 0 0-2-2h-2" stroke="currentColor" stroke-width="2" stroke-linecap="round"/></svg>
                Đơn hàng của tôi
            </a>
            <a href="/cart">
                <svg width="20" height="20" viewBox="0 0 24 24" fill="none"><path d="M6 2L3 6v14a2 2 0 0 0 2 2h14a2 2 0 0 0 2-2V6l-3-4z" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/></svg>
                Giỏ hàng
            </a>
            <a href="/auth/logout" style="color: var(--accent);">
                <svg width="20" height="20" viewBox="0 0 24 24" fill="none"><path d="M9 21H5a2 2 0 0 1-2-2V5a2 2 0 0 1 2-2h4M16 17l5-5-5-5M21 12H9" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/></svg>
                Đăng xuất
            </a>
        <% } else { %>
            <a href="/auth/login">
                <svg width="20" height="20" viewBox="0 0 24 24" fill="none"><path d="M15 3h4a2 2 0 0 1 2 2v14a2 2 0 0 1-2 2h-4M10 17l5-5-5-5M15 12H3" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/></svg>
                Đăng nhập
            </a>
            <a href="/auth/register">
                <svg width="20" height="20" viewBox="0 0 24 24" fill="none"><path d="M16 21v-2a4 4 0 0 0-4-4H5a4 4 0 0 0-4 4v2M8.5 11a4 4 0 1 0 0-8 4 4 0 0 0 0 8zM20 8v6M23 11h-6" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/></svg>
                Đăng ký
            </a>
        <% } %>
    </div>
</nav>

<main>
